<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Page</title>
    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css"
    rel="stylesheet"
/>
  </head>
  <style>
    * {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen,
    Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    overflow: hidden;
}

body {
  height: 100vh;
  width: 100vw;
  background: linear-gradient(135deg, #0f0c29 0%, #302b63 50%, #24243e 100%);
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
}

.container {
  position: relative;
  height: 100vh;
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: radial-gradient(circle at 50% 50%, rgba(20, 20, 40, 0.3) 0%, rgba(0, 0, 0, 0.8) 100%);
}

.planet-large {
  position: absolute;
  top: 0;
  right: 0;
  width: 800px;
  height: 800px;
  border-radius: 50%;
  background: linear-gradient(to bottom right, #9333ea, #4338ca, #1e40af);
  box-shadow: 0 0 120px rgba(147, 51, 234, 0.3);
  opacity: 0.7;
  transform: translate(33%, -25%);
}

.planet-small {
  position: absolute;
  top: 0;
  left: 0;
  width: 200px;
  height: 200px;
  border-radius: 50%;
  background: linear-gradient(45deg, #9333ea, #6366f1);
  box-shadow: 0 0 60px rgba(99, 102, 241, 0.2);
  opacity: 0.2;
  transform: translate(-50%, 25%);
}

.gradient-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(to top, 
    rgba(0, 0, 0, 0.9) 0%, 
    rgba(0, 0, 0, 0.7) 30%, 
    rgba(0, 0, 0, 0.4) 60%, 
    rgba(0, 0, 0, 0) 100%);
}

.stars {
  position: absolute;
  inset: 0;
  background-image: 
    radial-gradient(2px 2px at 20px 30px, #eee, rgba(0, 0, 0, 0)),
    radial-gradient(2px 2px at 40px 70px, #fff, rgba(0, 0, 0, 0)),
    radial-gradient(2px 2px at 50px 160px, #ddd, rgba(0, 0, 0, 0)),
    radial-gradient(2px 2px at 90px 40px, #fff, rgba(0, 0, 0, 0)),
    radial-gradient(2px 2px at 130px 80px, #fff, rgba(0, 0, 0, 0)),
    radial-gradient(2px 2px at 160px 120px, #ddd, rgba(0, 0, 0, 0)),
    radial-gradient(1px 1px at 200px 200px, #fff, rgba(0, 0, 0, 0)),
    radial-gradient(1px 1px at 300px 250px, #eee, rgba(0, 0, 0, 0)),
    radial-gradient(1px 1px at 400px 350px, #fff, rgba(0, 0, 0, 0));
  background-repeat: repeat;
  background-size: 500px 500px;
  opacity: 0.4;
  animation: twinkle 3s ease-in-out infinite alternate;
}

@keyframes twinkle {
  0% {
    opacity: 0.2;
  }
  100% {
    opacity: 0.5;
  }
}

.auth-container {
  position: relative;
  z-index: 10;
  width: 100%;
  max-width: 400px;
  padding: 0 20px;
  text-align: center;
}

.auth-form {
  background-color: rgba(41, 44, 49, 0.193);
  backdrop-filter: blur(10px);
  padding: 32px;
  border-radius: 8px;
  box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25),
              0 0 15px rgba(123, 97, 255, 0.2);
  border: 1px solid rgba(31, 41, 55, 0.8);

}

h2 {
  font-size: 24px;
  font-weight: 300;
  color: white;
  margin-bottom: 10px;
}

p {
  color: #9ca3af;
  font-size: 12px;
  margin-bottom: 24px;
}

.form-group {
  margin-bottom: 16px;
}

input {
  width: 100%;
  background-color: rgba(31, 41, 55, 0.5);
  color: white;
  border: 1px solid #374151;
  border-radius: 4px;
  padding: 8px 12px;
  font-size: 14px;
  outline: none;
  transition: border-color 0.3s, box-shadow 0.3s;
}

input:focus {
  border-color: #8b5cf6;
  box-shadow: 0 0 0 2px rgba(139, 92, 246, 0.2);
}

.password-group {
  position: relative;
  margin-bottom: 8px;
}

.password-toggle {
  position: absolute;
  right: 12px;
  top: 8px;
  background: none;
  border: none;
  color: #9ca3af;
  font-size: 15px;
  cursor: pointer;
}

.submit-button {
  margin-top: 25px;
  width: 70%;
  background: linear-gradient(to right, #8b5cf6, #6366f1);
  color: white;
  border: none;
  border-radius: 4px;
  padding: 8px 0;
  font-size: 14px;
  cursor: pointer;
  transition: all 0.3s;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
}

.submit-button:hover {
  background: linear-gradient(to right, #7c3aed, #4f46e5);
  box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  transform: translateY(-1px);
}

.divider {
  margin: 16px 0;
  text-align: center;
  color: #9ca3af;
  font-size: 14px;
  position: relative;
}

.divider::before,
.divider::after {
  content: '';
  position: absolute;
  top: 50%;
  width: 40%;
  height: 1px;
  background-color: rgba(156, 163, 175, 0.2);
}

.divider::before {
  left: 0;
}

.divider::after {
  right: 0;
}

.toggle-form {
  margin-top: 24px;
  text-align: center;
  font-size: 14px;
}

.toggle-link {
  text-decoration: none;
  background: none;
  border: none;
  color: #60a5fa;
  cursor: pointer;
  font-size: 14px;
  transition: color 0.2s;
}

.toggle-link:hover {
  color: #93c5fd;
  text-shadow: 0 0 8px rgba(147, 197, 253, 0.3);
}

@media (max-width: 768px) {
  .planet-large {
    width: 600px;
    height: 600px;
  }
  
  .planet-small {
    width: 150px;
    height: 150px;
  }
}

@media (max-width: 480px) {
  .auth-form {
    padding: 24px;
  }
  
  .planet-large {
    width: 400px;
    height: 400px;
  }
  
  .planet-small {
    width: 100px;
    height: 100px;
  }
}
  </style>
  <body>
    <div class="container">
      <div class="planet-large"></div>
      <div class="planet-small"></div>
      <div class="gradient-overlay"></div>
      <div class="stars"></div>
      

      <div class="auth-container">
        <div class="auth-form">
          <h2 id="form-title">Welcome Back</h2>
          <p id="form-subtitle">Login to continue your anonymous journey on TeenSYNC</p>
          
          <form id="auth-form" action="/api/v1/user/login" method="POST" >
            
            <div class="form-group">
              <input type="text" id="email" name="email" placeholder="Enter your email" required />
            </div>
            
            <div class="form-group password-group">
              <input type="password" id="password" name="password" placeholder="Password" required />
            
              <button type="button" class="password-toggle" onclick="togglePassword()">
              <i id="passToggler" class=" ri-eye-close-line"></i>
              </button>
            </div>


            <button type="submit" class="submit-button">
              Sign In
            </button>

          </form>
          
          <div class="divider">or</div>
          
          
          <div class="toggle-form">
            <p id="toggle-text">
              New to TeenSYNC? <a href="/sign-in" class="toggle-link">Sign up</a>
            </p>
          </div>
        </div>
      </div>
    </div>

    <script>
      let showPassword = false;
      const passwordInput = document.getElementById('password');
      const icon = document.getElementById('passToggler');
  
      function togglePassword() {
        showPassword = !showPassword;
        passwordInput.type = showPassword ? 'text' : 'password';
  
        if (showPassword) {
          icon.classList.remove('ri-eye-close-line');
          icon.classList.add('ri-eye-2-line');
        } else {
          icon.classList.remove('ri-eye-2-line');
          icon.classList.add('ri-eye-close-line');
        }
      }
    </script>
  </body>
</html>