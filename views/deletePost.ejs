<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Post Confirmation</title>
   <style>
    :root {
    --purple: #9333ea;
    --light-blue: #6366f1;
    --dark-blue: #302b63;
    --dark-gray: #374151;
    --medium-gray: #9ca3af;
    --white: #ffffff;
    --light-purple: #8b5cf6;
    --black: #0f0c29;
    --background: #0f172a;
    --surface: #1e293b;
    --surface-lighter: #334155;
    --text-primary: #f1f5f9;
    --text-secondary: #94a3b8;
    --border-color: #334155;
}

/* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* Background styling */
body {
    background: linear-gradient(to bottom, var(--background), var(--dark-blue));
    font-family: Arial, sans-serif;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
}

/* Centered Confirmation Box */
.confirmation-box {
    background-color: var(--surface-lighter);
    padding: 30px 50px;
    border-radius: 10px;
    width: 400px;
    text-align: center;
    box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.3);
}

/* Heading Text */
.confirmation-box h2 {
    color: var(--text-primary);
    font-size: 24px;
    margin-bottom: 20px;
}

/* Button Styling */
.button {
    padding: 10px 20px;
    font-size: 16px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    margin: 10px;
    transition: background-color 0.3s ease;
    color: var(--white);
}

/* Cancel Button */
.cancel-btn {
    background-color: var(--medium-gray);
    color: var(--black);
    border: 1px solid rgba(255, 255, 255, 0);
}

.cancel-btn:hover {
    background-color: var(--dark-gray);
    color: var(--text-primary);
    border: 1px solid var(--white);
}

/* Confirm Button */
.confirm-btn {
    background-color: var(--light-blue);
}

.confirm-btn:hover {
    background-color: var(--light-purple);
}

/* Flexbox for positioning buttons */
.buttons-container {
    display: flex;
    justify-content: space-between;
}
   </style>
</head>
<body>

    <div class="confirmation-box">
        <h2>Are you sure you want to delete this post?</h2>
        <div class="buttons-container">
            <button class="button cancel-btn" onclick="cancelDelete()">Cancel</button>
            <form action="/api/v1/post/delete/<%= postId %>" method="post">
                <button class="button confirm-btn" type="submit">Confirm</button>
            </form>
        </div>
    </div>

    <script>
        function cancelDelete() {
            window.location.href = '/profile';
        }
    </script>

</body>
</html>
